#!/bin/sh
# yo — yank output of last command to clipboard
# Clears its own line so it leaves no trace in the terminal.
printf '\033[1A\033[2K\r'
"$(dirname "$0")/../tmux/yank-last-output.sh" 2>/dev/null
